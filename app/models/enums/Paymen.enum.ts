export enum PaymentMethodEnum {
  PROMPTPAY = 'promptpay',
  CREDIT_CARD = 'card'
}

export enum PaymentStatusEnum {
  PENDING = 'pending',
  SUCCEEDED = 'succeeded',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired'
}
