export enum PaymentMethodEnum {
  PROMPTPAY = 'promptpay',
  CREDIT_CARD = 'card'
}

export enum PaymentStatusEnum {
  PENDING = 'pending',
  PAID = 'paid',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired'
}
