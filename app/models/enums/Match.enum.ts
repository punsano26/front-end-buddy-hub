export enum MatchEvent {
  FOUND = 'match:found',
  MESSAGE = 'match:message',
  EXPIRED = 'match:expired',
  PARTNER_LEFT = 'match:partner_left',
  FRIEND_REQUEST = 'match:friend_request',
  PERSISTED = 'match:persisted'
}
